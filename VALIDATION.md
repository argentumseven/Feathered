# Release verification

The release runner now reconciles the collected test IDs with setup, call and
teardown records from each pytest subprocess. A zero subprocess exit is necessary
but insufficient: missing, stale, incomplete or contradictory evidence fails the
gate. Normal pytest reporting and shutdown remain enabled.

## Per-run results

The authoritative result is generated by the runner, rather than maintained as a
handwritten test count:

- `validation/core-leaves-gate/SUMMARY.md`: human-readable result, actual outcome
  counts, commit, source-tree digest, Python/pytest versions and environment.
- `validation/core-leaves-gate/summary.json`: every final test outcome, allowed skip
  reason, batch reference and subprocess exit/signal/timeout result.
- `validation/core-leaves-gate/collection.json` and `batch-NNN.json`: collected IDs,
  collection problems, deselections, and individual setup/call/teardown reports.
- `validation/core-leaves-gate/*.log`: normal pytest failure explanations and output,
  retained even when a subprocess crashes or times out.

The source-tree digest includes source changes beyond the named commit. Generated
validation evidence is outside source-manifest scope, so writing a report does
not change the source identity it describes. `SOURCE-SHA256.json` itself is
generated after merges and is not tracked by Git. CI uploads the reports on failure
as well as success. Windows and the required Debian job verify the same Git SHA
before production publication is eligible.

## Independently established starting point

For item 1 of the implementation instruction, the unchanged application at
`7222d245ddfdaa84a2b191ae544898337052ed67` was run using the new reporting mechanism
loaded externally: **1,255 passed, zero skips**, with complete phase evidence.
The baseline checkout was not modified to obtain that count. Evidence is in
`validation/item1/baseline-outcomes/`; the recorder was supplied by the item-1
implementation, not by the baseline's defective exit plugin.

Three behavioral tests first failed against the original runner/plugin: skipped
tests were accepted, the required-test finish hook was bypassed, and assertion
diagnostics were suppressed. See `validation/item1/baseline-regressions.txt`.
The new gate tests also exercise phase failures, collection failure/skip,
deselection, xfail/xpass, early process exit, timeout, missing/stale reports,
long node IDs and normal shutdown. Current results are in the generated summary.

## Platform expectations

On Linux, the full corpus must pass with Tk/Xvfb and the fixture tools available.
No skip is accepted. The required Debian native-conformance job now runs this
complete batched gate in addition to its existing required integration slice.

Windows may record only the exact file/reason combinations listed in
`release_test_runner.WINDOWS_SKIPS`: missing dpkg fixtures, the named Bash checks,
the Linux-only installation checks, and the symlink-capability check. These are reported as skips, never as passed or
executed tests. Missing Tk/display, unrelated skips, collection skips, deselection,
xfail and xpass fail the gate. Every permitted Windows skip must execute in the
required Linux full-corpus job; production publication requires both jobs.

## Reproduction

```bash
python -m mypy
python check_host_contracts.py
ruff check .
python check_installer_syntax.py
python write_source_manifest.py
python verify_source_checksums.py
FEATHERED_USE_XVFB=1 python release_test_runner.py --report-dir validation/platform-gate
```

On Windows, omit `FEATHERED_USE_XVFB=1`. The runner defaults to a 300-second
collection/batch deadline; `--timeout SECONDS` overrides it explicitly. A timeout
fails the gate and stops the child process tree. `--report-dir PATH` selects a
different evidence directory. Test IDs are passed in JSON and source paths in a
pytest argument file, avoiding command-line limits without omitting parameters.

## Current identity and differential-planning batch

Three leaf responsibilities now live outside core: payload destination identity,
baseline digest comparison, and RPM Python capability spelling. Core adapters
preserve old imports and dependency hooks. File access/reporting and version
comparison remain in their prior owners. See `CORE-LEAVES-CHANGES.md` for scope,
compatibility cases, measured overhead and remaining work.

The new contract slice passes against both the untouched starting implementation
and this tree. Existing provider and bundle-output goldens are unchanged. All
three modules join strict typing and Tk-blocked imports; Mypy covers 59 roots.
The final gate is `validation/core-leaves-gate/SUMMARY.md`; focused evidence and
benchmark data are under `validation/core-leaves/`.

## Previous provider-index and core-leaf batch

Instruction 8.2 was profiled before changing code. A 12,000-record synthetic RPM
catalog with 16 constrained roots rebuilt indexes 36 times; the updated resolver
builds six across the same public transaction. The constrained index is shared
only within one pass. Unprofiled median timing was 17.709 seconds before and
3.348 seconds after; ordinary and single-constrained-root cases retain their
original index-build counts. See `PROVIDER-INDEX-CHANGES.md` for scope and limits.

Eight pre-change resolver snapshots preserve root/source selection and reports;
regressions check empty indexes and isolation across constraints, metadata changes
and cancellation. The first instruction-9 extraction consolidates raw hashing
loops behind unchanged core entry points. Sixteen compatibility tests passed
before and after extraction; the new module joins the strict typing and Tk-free
import gates. Focused evidence is in `validation/provider-index/`; the complete
result for that prior tree is `validation/provider-index-gate/SUMMARY.md`.

## Previous signing-navigation and digest-reuse batch

The signing checkbox and Next now reveal the signing-key field in Provenance &
Keying, or its OpenPGP setup card when the field is disabled. Real App tests
first reproduced the incorrect Folder Naming destination, then passed after
repair. No key is selected automatically and verification policy is unchanged.

Instruction 8.1 now reuses SHA-256 within a single publication across payload
verification, cache population, manifest, checksum-list and provenance records.
All three backend output/event fixtures remain unchanged. File mutation,
replacement, failed verification, nested/concurrent scopes and independent final
sealing have explicit regression coverage. Windows uses FILE_BASIC_INFO change
time rather than creation time; missing capability disables reuse. Its native
execution still requires the existing Windows gate.

See `SIGNING-DIGEST-CHANGES.md` for measured results and limitations. Focused
logs and the reproducible benchmark are in `validation/signing-digests/`;
`validation/signing-digest-gate/SUMMARY.md` records that prior tree.
The initial post-fix display-harness failure produced six skips; that diagnostic
run is not counted as a pass. The repaired display run executed all six GUI cases.

## Previous platform and dispatch batch

This batch starts from `720749d28ba8011a6d1e8f0f17430981e5080bb8` without fetching
or merging upstream changes. It completes typed backend dispatch, adds Linux
GUI/CLI installation and launch checks, and improves Windows bootstrap quoting,
current-process Tcl/Tk configuration and retained diagnostics.
See `PLATFORM-CHANGES.md` and `LINUX.md` for scope and limitations. Current evidence
is in `validation/platform/` and `validation/platform-gate/`. The latter records
the source-tree digest and any tracked source edits independently of the base SHA.

## Previous shared-writer batch

The current batch begins at `aa3a685f4725f7b12c2d96be252a77c94097ad12`.
It adds characterized, shared acquisition and record writing for all three package
families, APT compatibility delegates and an Arch cache/reporting repair.
`BUNDLE-WRITER.md` describes preserved behavior and the intentional change.
Focused, before-fix and static evidence is in `validation/bundle-refactor/`;
that batch's complete-corpus result is in `validation/bundle-refactor-verified/`.
Intermediate failed runs are retained under `validation/bundle-delegation-gate/`
and `validation/bundle-delegation-gate-rerun/`, with their causes documented.

## Previous build-boundary batch

This batch starts at upstream `b14bf5e0de840269289e5c2e728f1e17e26c39a7`.
The unmodified source passed 1,341 local tests with no skips or deselections.
Baseline and focused evidence are in `validation/build-boundary/`; that batch's
full-tree evidence is in `validation/build-boundary-gate/`. Earlier reports in
`validation/release-gate/`, `item1/` and `batch25/` are historical evidence.

New coverage checks direct plan ownership, all three inventory types, ordered
execution feedback, cancellation and terminal outcomes. A real GUI build is
compared with a CLI replay for exact payload hashes, selected identities,
source-policy records, provenance (excluding its generated ID and creation
instant), and installer bytes. Existing Tk-blocked, native-family orchestration,
mirror, GUI locking and replay tests remain in the complete release gate.
See `BUILD-BOUNDARY.md` for the typing and compatibility scope.

GitHub run 34737027821 on the baseline passed its native APT/DNF/pacman and static
analysis jobs but failed the Windows Python/Tk bootstrap before application tests.
Only its exit-code annotation was publicly available during this review; detailed
job log access returned HTTP 403. This batch does not claim to fix or certify
that bootstrap. A local Linux pass does not establish Windows release readiness.

## Scope of local validation

The follow-on batch implements instruction items 2-5: credential-safe display
with source URLs visible, strict decoding of recognized spec fields and runtime
preparation inputs, partial-outage recovery, shared release-knowledge ownership,
and bounded UI event processing with incremental query coordination. Each
behavioral repair has focused regression evidence in `validation/batch25/`;
the complete final-tree result remains `validation/core-leaves-gate/SUMMARY.md`.
Malformed specs formerly accepted by truthiness are intentionally rejected;
valid older specs and incomplete drafts remain supported. See
`BACKGROUND-REFRESH.md` for cache ownership, cancellation and migration scope.

The new heartbeat test demonstrated queue starvation under a synthetic progress
burst. It is a reproduction of that bounded scenario, not a claim that every
historical UI hang had the same cause. No tests are deliberately deselected in
the delivered full release gate. Diagnostics during development included a
headless focused run and a display-harness setup failure; those are not counted
as GUI validation.

The implementation review uses Linux, Python 3.12.14, pytest 9.1.1 and Tk/Xvfb.
Mypy and Ruff use the configured versions, 1.18.2 and 0.14.2. Local execution does
not establish Windows execution, Authenticode signing, frozen production builds,
staged gpgv behavior, native package-manager oracle acceptance or live upstream
availability. Those checks were not performed for this batch. The
existing CI requirements for native conformance and signed production remain in
place; a workflow definition is not evidence that its remote job has run.

Item 1 changed only release verification. The follow-on batch changes application
input validation and discovery/event handling; it does not change package
resolution, bundle layout, stored repository credentials or transport identity.
