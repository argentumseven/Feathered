[pytest]
# Collect the top-level regression modules and the tests/ package.
testpaths = .
python_files = test_*.py
# validation/ holds generated gate output, not tests.
norecursedirs = validation build dist .git .venv venv .release-venv __pycache__ node_modules
