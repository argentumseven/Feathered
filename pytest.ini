[pytest]
# Feathered's suite is one module; make it discoverable by a bare `pytest` so a
# CI job running the ordinary command cannot pass without executing it.
testpaths = .
python_files = test_*.py
